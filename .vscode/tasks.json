{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build libtuxrup_gtk4.so",
            "type": "shell",
            "command": "cmake .. && make",
            "options": {
                "cwd": "${workspaceFolder}/tuxrup/build"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
